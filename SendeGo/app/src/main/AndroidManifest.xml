<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--Permisos para poder utilizar diferentes servicios-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
            <intent>
                <action android:name="android.intent.action.VIEW" />
                <data android:scheme="geo" />
            </intent>

    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="SendeGo!"
        android:icon="@mipmap/logo_sendego_asset"
        android:roundIcon="@mipmap/logo_sendego_asset"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.SendeGo">

        <!-- ACTIVIDADES -->

        <activity
            android:name=".PointsOfInterestAdapter"
            android:exported="false"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".activities.PointOfInterestRegistration"
            android:exported="false" />
        <activity
            android:name=".activities.AboutUs"
            android:exported="false" />
        <activity
            android:name=".activities.RouteRegistration"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SERVICIOS -->

        <service android:name=".services.MusicService" android:enabled="true">
        </service>

    </application>

</manifest>